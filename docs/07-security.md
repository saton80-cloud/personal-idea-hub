# 安全说明

- 客户端从不直接连接MySQL，只能访问HTTP API。
- MySQL仅监听服务器本机，防火墙不开放3306。
- PC账号登录后获得设备独立令牌。
- 二维码使用随机高熵、5分钟、一次性票据。
- 服务端只保存令牌和票据的SHA-256摘要。
- 用户密码使用PBKDF2-SHA512和独立随机盐。
- 每次同步校验令牌所属用户与设备。
- 所有查询按`owner_id`隔离。

当前系统仅限可信局域网。若未来跨公网使用，必须增加HTTPS证书、限流、审计、远程设备撤销和外部安全测试。
